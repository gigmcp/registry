schemaVersion: 1
name: semrush
version: 0.1.0
source:
  repo: github.com/gigmcp/semrush-mcp
  tag: v0.1.0
image:
  ref: ghcr.io/gigmcp/semrush-mcp
  # PLACEHOLDER until build-images CI emits the real digest — see README.
  digest: sha256:7628485ab18df42253770a555539b6a08fc4bc4f18c58b302eafe95435bd0d18
  entrypoint: /app/server
# Semrush Analytics API accepts the API key only as a query parameter
# (?key=...); header injection cannot work, so entrusted tier.
tier: entrusted
entitlements:
  egress:
    - api.semrush.com
credentials:
  - id: semrush_api_key
    type: api_key
    provider: semrush
    inject:
      env: SEMRUSH_API_KEY
tools:
  - name: get_domain_organic_search_keywords
    default: true
  - name: get_domain_paid_search_keywords
    default: true
  - name: get_backlinks
    default: true
  - name: get_referring_domains
    default: true
  - name: get_keyword_overview
    default: true
  - name: get_keyword_difficulty
    default: true
  - name: get_competitors_in_organic_search
    default: true
  - name: get_related_keywords
    default: true
  - name: check_account_units_balance
    default: true
