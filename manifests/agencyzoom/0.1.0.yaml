schemaVersion: 1
name: agencyzoom
version: 0.1.0
source:
  repo: github.com/gigmcp/agencyzoom-mcp
  tag: v0.1.0
image:
  ref: ghcr.io/gigmcp/agencyzoom-mcp
  # PLACEHOLDER until build-images CI emits the real digest — see README.
  digest: sha256:4691abc7df762172cd4d6eabc7be3776032abefd4646aa711a05c996dd236495
  entrypoint: /app/server
tier: sealed
entitlements:
  egress:
    - api.agencyzoom.com
credentials:
  - id: agencyzoom_token
    type: api_key
    provider: agencyzoom
    inject:
      header: Authorization
      format: "Bearer {token}"
tools:
  - name: search_leads
    default: true
  - name: get_lead_details
    default: true
  - name: search_customers
    default: true
  - name: get_customer_details
    default: true
  - name: search_and_list_tasks
    default: true
  - name: get_pipelines
    default: true
  - name: create_lead
    default: false
  - name: update_lead
    default: false
  - name: create_task
    default: false
  - name: create_customer_note
    default: false
