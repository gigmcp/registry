schemaVersion: 1
name: tinypng
version: 0.1.0
source:
  repo: github.com/gigmcp/toolpack
  tag: v0.1.0
image:
  ref: ghcr.io/gigmcp/tinypng-mcp
  digest: sha256:b748e61dc475eaacffc3e40cc37012065f18255857c57c3a5fdd012aae2624ec
  entrypoint: /app/server
  builder: toolpack
tier: sealed
entitlements:
  egress:
    # Tinify (TinyPNG) image compression API.
    - api.tinify.com
credentials:
  - id: tinypng_api_key
    # Tinify uses HTTP Basic auth: user "api", password = API key;
    # {token} is the base64("api:<key>") credential.
    type: basic
    provider: tinypng
    inject:
      header: Authorization
      format: "Basic {token}"
tools:
  - name: get_compression_count
    default: true
  - name: download_compressed_image
    default: true
  - name: shrink_image
    default: false
  - name: transform_image
    default: false
  - name: compress_and_store_in_azure
    default: false
